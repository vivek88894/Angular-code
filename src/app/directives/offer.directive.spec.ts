import { OfferDirective } from './offer.directive';

describe('OfferDirective', () => {
  it('should create an instance', () => {
    const directive = new OfferDirective(null,null,null);
    expect(directive).toBeTruthy();
  });
});
